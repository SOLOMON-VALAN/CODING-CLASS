n=int(input("Enter number of elements : "))
l=list(map(float,input("Enter elements : ").split()))
res=[]
for i in range(0,n):
    count=0
    for j in range(0,n):
        if l[i]==l[j]:
            count=count+1
    if count!=3:
        res.append(l[i])
print(res)
